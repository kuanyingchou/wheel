# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestWheel' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestWheel
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'

  target 'TestWheelTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestWheelUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
